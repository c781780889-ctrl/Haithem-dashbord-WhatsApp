# تقرير استبدال إضافة حسابات Telegram

## النتيجة

تم استبدال نافذة إضافة الحساب القديمة التي كانت تطلب `API ID` و`API Hash` و`Session String` بواجهة مصادقة حقيقية تبدأ برقم الهاتف، ثم تطلب رمز Telegram، وتتعامل مع التحقق بخطوتين عند الحاجة.

التدفق أصبح: رقم الهاتف → طلب كود فعلي من GramJS/Telegram → إدخال الكود → 2FA عند الحاجة → إنشاء Session داخل Telegram → تشفيرها في Backend → إنشاء سجل الحساب → تشغيل Telegram Worker الموجود → عرض الحساب بعد الاتصال الفعلي.

## ما تم تنفيذه

| المكون | التنفيذ |
|---|---|
| رقم الهاتف | تطبيع دولي والتحقق من `+` والدولة والأرقام فقط |
| طلب الكود | `POST /api/v1/telegram/auth/request-code` ويستدعي GramJS فعلياً |
| التحقق | `POST /api/v1/telegram/auth/:id/verify-code` دون حفظ الكود |
| 2FA | `POST /api/v1/telegram/auth/:id/verify-2fa` دون حفظ كلمة المرور |
| حالة المصادقة | جلسة مؤقتة في PostgreSQL مع انتهاء زمني وحالات واضحة |
| منع التكرار | قيد جلسة نشطة حسب المستخدم والرقم + Rate Limit للخادم |
| الجلسة | AES-256-GCM باستخدام `SESSION_ENCRYPTION_KEY` من Environment |
| الحساب | ينشأ بعد نجاح Telegram فقط، وليس بعد نجاح HTTP شكلي |
| العامل | يُسلّم الحساب إلى `TelegramService` الحالي ولا ينشئ Manager ثانياً |
| الاستمرار | Worker يعيد تحميل الجلسة المشفرة بعد Restart |
| الواجهة | RTL، رقم، كود، 2FA، عدّاد انتهاء، إلغاء، وإعادة البدء |
| الأسرار | لا تُعاد Session أو API Hash أو كلمة المرور أو الكود إلى الواجهة |

## متطلبات التشغيل

يجب ضبط متغيري الخادم `TELEGRAM_API_ID` و`TELEGRAM_API_HASH`، إضافة إلى `SESSION_ENCRYPTION_KEY` ثابت وطويل في Railway/Secret Manager. لا ينبغي تغيير مفتاح التشفير بين عمليات النشر، لأن الجلسات القديمة تعتمد عليه.

الحسابات الجديدة لا تخزن API Hash داخل سجل الحساب؛ TelegramService يستخدم القيم الموجودة في Environment. الجلسات الجديدة تحفظ في `session_encrypted` فقط، بينما تبقى الجلسات القديمة المدخلة بالنظام السابق مدعومة للتوافق أثناء الانتقال.

## الاختبارات

تم اجتياز اختبار تطبيع رقم الهاتف وتشفير وفك تشفير الجلسة مع التأكد من اختلاف النص المشفر عن النص الأصلي، وفحوص صياغة ملفات Backend والـRoutes والـMigrations، وفحص `git diff --check`، وبناء الواجهة Production بنجاح وظهور `TelegramView` في ناتج البناء.

لم يتم الادعاء باختبار إرسال كود حقيقي أو إدخال OTP فعلي من Telegram داخل بيئة التدقيق؛ ذلك يتطلب قيم Environment صحيحة وحساب Telegram فعلياً وإجراء المستخدم للكود. بعد النشر، يجب اختبار رقم حساب غير حساس والتأكد من وصول الكود، حالة 2FA، ظهور Connected، ثم Restart للخادم والتحقق من عودة الحساب دون كود جديد.
