# تصميم مسار الانضمام والنشر المباشر

## ترتيب الواجهة

1. اختيار الحسابات المؤهلة وإظهار الحالة الحية لكل حساب.
2. اختيار إعلان واحد أو عدة إعلانات من مكتبة الحساب الأول المحدد؛ تبقى المكتبة مصدر البيانات الوحيد.
3. استيراد روابط ومراجعتها قبل التشغيل.
4. ضبط ثلاثة تأخيرات مستقلة: بعد الانضمام، وبعد النشر، وبعد الخروج، مع خيار تفعيل الخروج.
5. معاينة ثابتة للإعدادات ثم بدء أو إيقاف مؤقت أو استئناف أو إيقاف كامل أو إعادة ضبط محلية.
6. عرض سجل مرحلة كل عملية وحالة الرابط والحساب والخطأ إن وجد.

## نموذج المهمة

تخزن المهمة الحسابات والروابط والإعلانات المختارة والتأخيرات وقرار الخروج في قاعدة البيانات. لكل علاقة حساب × رابط عملية مستقلة ذات حالة عامة وحالات مراحل: `join_status` و`publish_status` و`leave_status`، إضافة إلى المعرف الفعلي للمجموعة والطوابع الزمنية للأحداث.

## تسلسل التنفيذ لكل عملية

`الرابط → الانضمام → انتظار بعد الانضمام → النشر → انتظار بعد النشر → الخروج الاختياري → انتظار بعد الخروج → اكتمال`.

لا تُجدول عملية الرابط التالية للحساب نفسه إلا بعد اكتمال العملية الحالية؛ ويحمي قفل الحساب هذا الترتيب حتى عند الاستئناف أو إعادة محاولة حالة مؤقتة. تمر مرحلة النشر عبر `sendMessageSafe`، ولا تُنفذ أي محاولة لتجاوز الحظر أو CAPTCHA أو قيود المنصة.

## الصيغ المدعومة

يُوحّد الاستيراد عبر مسار واحد يدعم `.docx` و`.txt` و`.csv` و`.xlsx`، ويستخرج الروابط من النص/الخلايا ثم يمررها إلى معالج التحقق والتوحيد وإزالة التكرار الحالي.
