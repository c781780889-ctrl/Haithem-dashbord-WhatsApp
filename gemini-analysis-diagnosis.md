# تقرير تشخيص دورة تحليل Gemini

## النتيجة التنفيذية

أظهر الفحص وجود خلل حقيقي في دورة الحالات والتشخيص، وليس دليلًا على أن Gemini كان يعمل لمجرد ظهور عبارة «بانتظار تحليل Gemini». كان المسار الخلفي ينشئ السجل ويضعه في Queue عند تفعيل Gemini، ثم يعالج المهمة عبر BullMQ ومعالج `analyze_telegram_smart_conversation`. لكن الواجهة كانت تختزل الحالات `pending` و`processing` و`retrying` كلها إلى عبارة واحدة، كما لم يكن Backend يرسل حدث Realtime عند بدء المعالجة أو عند الفشل. إضافة إلى ذلك، عند فشل الإضافة إلى Queue كان Backend يسجل الفشل ثم يرسل مباشرة حدثًا لاحقًا يعيد البطاقة ظاهريًا إلى «بانتظار تحليل Gemini».

تم إصلاح هذه النقاط. أصبحت الواجهة تميّز بين «بانتظار تحليل Gemini»، و«جاري تحليل Gemini»، و«إعادة محاولة تحليل Gemini»، و«تم التحليل»، و«فشل التحليل»، وتعرض `ai_error` صراحة. وأصبح Backend يرسل تحديثًا عند بدء المعالجة، ويسجل الفشل النهائي على أنه `failed` مع `status='analysis_failed'` وسبب واضح، ويسجل المحاولات غير النهائية كـ`retrying`. كما تم منع حدث الانتظار من الكتابة فوق فشل enqueue.

## المسار الذي تم تتبعه

| المرحلة | التنفيذ الموجود | نتيجة المراجعة |
|---|---|---|
| استقبال المحادثة | `TelegramService` يستدعي `TelegramSmartConversationService.ingest` | موجود فعليًا |
| حفظ المحادثة | إدراج في `telegram_smart_results` | موجود؛ الحالة الأولية `ai_status='pending'` |
| إنشاء المهمة | `QueueManager.enqueueGeminiAnalysis` | موجود عند كون Gemini مفعّلًا ومضبوطًا |
| إرسال المهمة للعامل | Queue `gemini-analysis` ومعالج `analyze_telegram_smart_conversation` | موجود في bootstrap؛ يعتمد على Redis/BullMQ |
| إرسال النص إلى Gemini | `GeminiAIService.analyze` باستخدام `@google/genai` | موجود؛ النموذج الافتراضي `gemini-2.5-flash` |
| استلام النتيجة | تحليل JSON والتحقق من المخطط | موجود؛ الأخطاء تُصنّف وتُعاد محاولتها حسب السياسة |
| حفظ نتيجة التحليل | تحديث `telegram_smart_results` إلى `completed` | موجود |
| تحديث Dashboard | Socket event ثم إعادة تحميل البيانات | موجود عند الانتظار والإكمال، وأضيف الآن عند المعالجة والفشل |

## السبب الجذري المثبت من الكود

السبب الجذري المؤكد هو **خلل في تمثيل الحالات وفي ترتيب أحداث Realtime عند فشل enqueue**. كانت البطاقة تستخدم شرطًا واحدًا يعتبر كل `pending` و`processing` و`retrying` حالة انتظار، لذلك لم يكن ظهور العبارة يثبت أن المهمة لم تصل إلى العامل. كذلك كان مسار `catch` الخاص بفشل Queue يحدّث قاعدة البيانات إلى `ai_status='failed'`، ثم يواصل التنفيذ ويرسل حدثًا جديدًا ببيانات `ai_status='pending'`؛ وهذا قد يخفي الفشل في الواجهة.

يوجد أيضًا اعتماد تشغيلي واضح: إنشاء العامل يتطلب `REDIS_URL`، وإرسال Gemini يتطلب `GEMINI_ENABLED=true` وأحد المفتاحين `GEMINI_API_KEY` أو `GOOGLE_API_KEY`. إذا غاب Redis فلن يبدأ BullMQ، وإذا غاب المفتاح أو كان Gemini غير مفعّل فلن يدخل المسار الخارجي أصلًا، بل يستخدم النظام المحلل المحلي. هذه الشروط لا يمكن إثبات حالتها في الإنتاج من نسخة GitHub وحدها.

## الإصلاحات

تم تعديل `backend/src/api/services/TelegramSmartConversationService.js` بحيث يرسل حدثًا عند الانتقال إلى `processing`، ويحفظ سبب الفشل في `ai_error`، ويستخدم `retrying` للمحاولات غير النهائية و`failed` للفشل النهائي، ويغيّر الحالة إلى `analysis_failed` عند الفشل النهائي، ويمنع إرسال حالة pending بعد فشل enqueue.

تم تعديل `frontend/src/views/TelegramSmartConversationsView.tsx` بحيث يعرض الحالة الفعلية بدل العبارة الموحدة، ويعرض سبب الفشل، ويدعم الحالات `completed` و`processing` و`retrying` و`failed` و`dead_letter` بصورة قابلة للتشخيص.

تمت إضافة `backend/src/api/services/TelegramSmartConversationService.flow.test.js` لاختبار دورة الخدمة من الإدخال إلى إنشاء المهمة ثم المعالجة وإكمال النتيجة، مع التحقق من أحداث `pending → processing → completed`.

## نتائج الاختبار

| الاختبار | النتيجة |
|---|---|
| اختبارات Backend الكاملة بعد الإصلاح | ناجحة |
| اختبارات نطاق قواعد المحادثات الذكية | 8 اختبارات ناجحة |
| اختبار دورة الإدخال والمعالجة والإكمال | ناجح بعد عزل الاعتماد غير المرتبط `TelegramService` |
| بناء Frontend عبر Vite | ناجح |
| `git diff --check` | ناجح دون أخطاء تنسيق |
| اتصال Gemini الحقيقي من هذه الجلسة | غير قابل للإثبات؛ بيئة الجلسة لا تحتوي `DATABASE_URL` أو `REDIS_URL` أو إعدادات Gemini |

## ما لم أعتبره مثبتًا

لم أعتبر Gemini متصلًا أو أن محادثة إنتاجية محددة أُنجز تحليلها، لأن تشغيل Backend الحقيقي يحتاج بيانات قاعدة البيانات وRedis ومفتاح Gemini الموجود في بيئة النشر. لذلك لا يصح الجزم من هذا الفحص وحده بأن سبب السجلات الحالية هو API Key أو Rate Limit أو Timeout أو نموذج غير متاح. الإصلاح يضمن أن كل واحد من هذه الأسباب سيظهر كـ`ai_error` وحالة فشل قابلة للتشخيص بدل أن يبقى مقنّعًا كحالة انتظار.

لإتمام اختبار الإنتاج الذي طلبته حرفيًا، يجب تشغيل النسخة المعدلة في نفس بيئة النشر التي تحتوي `DATABASE_URL` و`REDIS_URL` و`GEMINI_ENABLED=true` ومفتاح Gemini، ثم إرسال محادثة حقيقية غير وهمية ومراقبة السجل والـQueue ونتيجة البطاقة. عندها يجب أن يظهر التسلسل `pending → processing → completed`، أو `pending → processing → retrying → failed` عند تعذر Gemini، مع حفظ السبب في `ai_error`.
