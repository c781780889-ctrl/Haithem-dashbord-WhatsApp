# أتمتة الانضمام لروابط WhatsApp

## سجل المشاكل والإصلاحات الكامل

| الحقل | القيمة |
|---|---|
| المشروع | `x781780889-jpg/whatsapp-dashboard-new` |
| الفرع | `main` |
| نطاق التوثيق | البحث عن روابط دعوات WhatsApp، إنشاء مهام الانضمام، Queue وOutbox، تنفيذ الانضمام، التحقق، الإحصاءات، والمراقبة |
| تاريخ التحديث | 25 أغسطس 2026 |
| آخر Commit مرتبط | [`d0e46c3`](https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/d0e46c3) |
| الحالة | الإصلاحات البرمجية مرفوعة إلى GitHub، وتحتاج بيئة Railway إلى إكمال النشر قبل التحقق الحي |

> **مهم:** هذا الملف يشرح ما تم تغييره في الكود وما تثبته الاختبارات المحلية. لا يُعتبر نجاح رابط معين مثبتًا في بيئة الإنتاج إلا بعد ظهور أحداث التنفيذ الفعلية وحقول الحالة في قاعدة البيانات.

---

## 1. الملخص التنفيذي

كانت أتمتة الانضمام تمر بعدة مراحل مستقلة: استخراج الرابط والتحقق من صيغته، حفظ الرابط، إنشاء عملية لكل علاقة حساب × رابط، وضع العملية في Outbox، إرسالها إلى Queue، تشغيل Worker، إرسال طلب الانضمام إلى WhatsApp، التحقق من العضوية، ثم تحديث حالة العملية والرابط والإحصاءات. معظم الأعراض التي ظهرت في اللوحة كانت نتيجة عدم تطابق هذه المراحل أو عدم وضوح الفرق بين نجاح الجدولة ونجاح الانضمام الفعلي.

تمت معالجة المسار على مراحل متتابعة. شملت الإصلاحات جعل التنفيذ الحقيقي يمر عبر Queue دائمة، منع تكرار العمليات، استعادة العمليات العالقة، حماية الحساب عند ظهور إشارات تقييد، تحسين فحص جاهزية الحساب، فصل صلاحية الرابط عن نجاح الانضمام، حفظ نتيجة الانضمام في الجداول التي تعتمد عليها اللوحة، جعل حماية الحد اليومي اختيارية، ثم إضافة حماية صريحة للفاصل الزمني بين انضمامات الحساب نفسه.

آخر إصلاحين يرتبطان مباشرة بالمشكلتين اللتين ظهرتا في الصور. أولًا، إذا قبل WhatsApp الدعوة وأعاد معرف المجموعة لكن تأخرت بيانات الأعضاء، فلا تُسجل العملية فشلًا زائفًا. ثانيًا، لا يمكن لعمليتين للحساب نفسه أن تبدآ قبل مرور الحد الأدنى المحدد، حتى لو جاءتا من مهمتين مختلفتين أو من Queue متزامنة.

---

## 2. دورة التنفيذ الصحيحة

المسار المقصود حاليًا هو أن يقوم المستخدم باختيار حسابات WhatsApp وروابط الدعوات، ثم ينشئ الخادم مهمة دائمة وسجلات عمليات مستقلة. لا ينفذ المتصفح الانضمام مباشرة؛ بل تُرسل العملية إلى Outbox ثم إلى Queue، وبعدها يستلمها Worker ويبدأ طلب WhatsApp. هذا الفصل يسمح باستمرار التنفيذ حتى عند إغلاق صفحة المتصفح، كما يسمح بتتبع كل مرحلة من خلال الأحداث والسجلات.[1] [2]

```text
رابط دعوة
   ↓
فحص الصيغة والتطبيع
   ↓
whatsapp_links + link_import_links
   ↓
link_import_tasks
   ↓
link_import_operations
   ↓
link_import_outbox
   ↓
Queue: wa-link-imports
   ↓
Worker
   ↓
groupAcceptInvite داخل جلسة WhatsApp
   ↓
حفظ نتيجة الانضمام والتحقق
   ↓
تحديث العملية والرابط والمهمة والإحصاءات
```

| المرحلة | الجدول أو المكوّن | النتيجة المتوقعة |
|---|---|---|
| استيراد الرابط | `link_import_links` و`whatsapp_links` | حفظ رابط صالح موحدًا دون تكرار |
| إنشاء المهمة | `link_import_tasks` | تعريف عدد العمليات والإعدادات |
| إنشاء العمليات | `link_import_operations` | عملية مستقلة لكل حساب × رابط |
| الجدولة الدائمة | `link_import_outbox` وBullMQ | عدم فقدان العملية عند إعادة تشغيل الخادم |
| التنفيذ | `GroupJoinerService` | استدعاء WhatsApp الحقيقي |
| التحديث | `LinkImportService` | تحديث `status` و`join_status` و`joined` وحقول التحقق |
| العرض | `DirectPublishSection` و`WhatsAppJoinAutomationView` | عرض النجاح والفشل والفاصل بالثواني |

---

## 3. المشاكل التي تم تشخيصها وإصلاحها

### 3.1 الخلط بين صلاحية الرابط ونجاح الانضمام

**المشكلة:** كان الرابط يظهر صالحًا لأن بنيته تتوافق مع صيغة دعوات WhatsApp، بينما لا يثبت ذلك أن المجموعة موجودة أو أن الدعوة ما زالت فعالة أو أن الحساب يستطيع الانضمام. لذلك كان من الممكن أن يرى المستخدم عددًا كبيرًا من الروابط الصالحة مع وجود عمليات انضمام فاشلة.

**الإصلاح:** تم فصل الحقول والمراحل. فـ`status` أو `validation_status` يصف صلاحية الرابط من الناحية الشكلية، بينما تصف `join_status` و`operation.status` نتيجة التنفيذ الفعلية. كما أصبحت تفاصيل الخطأ مرتبطة بالعملية والحساب والرابط بدل اعتبار الرابط نفسه فاشلًا قبل تنفيذ WhatsApp.[3]

| قبل الإصلاح | بعد الإصلاح |
|---|---|
| «الرابط صالح» يُفهم كأنه قابل للانضمام حتمًا | «الرابط صالح» يعني أنه اجتاز فحص الصيغة فقط |
| لا يوجد فصل واضح بين فحص الرابط وتنفيذ الانضمام | لكل مرحلة حالة مستقلة |
| صعوبة معرفة سبب الفشل | حفظ `last_error` و`error_code` وأحداث التنفيذ |

### 3.2 فقدان العمليات أو توقفها عند Outbox وQueue

**المشكلة:** كانت العملية قد تُحفظ أو تُدرج في Queue دون أن يكون هناك مسار موثوق لإعادة إرسالها بعد إعادة تشغيل الخادم أو عند فشل الإرسال. وكانت اللوحة تعرض وصول العملية إلى Queue دون أن يعني ذلك أن Worker نفذ طلب WhatsApp.

**الإصلاح:** تم اعتماد Outbox دائم وجدولة مستقلة لعمليات الروابط. أصبح لكل عملية سجل في `link_import_outbox`، ويقوم `dispatchOutbox` بإرسالها إلى Queue مع حالة ومحاولة ووقت إتاحة. كما أُضيف Dispatcher دوري يعيد معالجة سجلات Outbox القابلة للإرسال، مع تسجيل أحداث مثل `queue_outbox_created` و`queue_enqueued`.[4] [5]

> إنشاء Outbox أو ظهور `queue_enqueued` لا يثبت نجاح الانضمام. الدليل يبدأ من `job_received` ثم `job_started` ثم `join_started` و`join_result_received`.

### 3.3 عدم استعادة العمليات العالقة بعد إعادة التشغيل

**المشكلة:** قد تبقى العملية في حالة `processing` إذا توقف Worker أو انتهت صلاحية lease أثناء التنفيذ، فتظهر في اللوحة كأنها قيد المعالجة إلى أجل غير محدد.

**الإصلاح:** تم إضافة Recovery Watchdog يفحص العمليات التي انتهت leases الخاصة بها. يحاول النظام أولًا التأكد من العضوية الحالية، ثم يعيد العملية إلى `retry` عند الحاجة، ويعيد جدولة العمليات المعلقة التي لم تصل إلى Queue. كما تم حفظ `heartbeat_at` و`lease_expires_at` و`recovery_count` لتقليل العمليات العالقة غير القابلة للتشخيص.[6] [7]

### 3.4 إنشاء Jobs مكررة عند الإعادة أو إعادة تحميل الصفحة

**المشكلة:** إعادة إرسال الطلب أو إعادة المحاولة اليدوية قد ينشئ أكثر من Job للعملية نفسها، خصوصًا عند وجود اتصال بطيء أو إعادة تحميل لوحة التحكم.

**الإصلاح:** تم استخدام `Idempotency-Key` عند إنشاء البحث والمهام، ومفتاح ثابت لكل علاقة حساب × رابط داخل العملية. كما أصبحت Jobs المعاد إدراجها تحمل معرفات فريدة، وأضيفت قيود تمنع تكرار السجل نفسه في المهمة نفسها.[8] [9]

### 3.5 عدم تطابق بيانات العملية بين Outbox وWorker

**المشكلة:** كانت بعض البيانات تُحفظ بأسماء أو بنية تختلف عن الحقول التي يقرأها Worker، ما يؤدي إلى وصول Job إلى العامل دون معلومات كافية عن `operationId` أو الحساب أو الرابط.

**الإصلاح:** تم توحيد Payload العملية عند إنشائها في Outbox وعند إعادة إدراجها، وربطها صراحة بـ`taskId` و`operationId` و`accountId` و`linkId`. كما أصبح Worker يسجل `jobId` و`workerId` داخل العملية والأحداث.[10]

### 3.6 عدم ظهور الانضمام الناجح في عداد «مكتملة»

**المشكلة:** كان الانضمام يحدث فعليًا داخل حساب WhatsApp، لكن اللوحة لا تزيد عداد المكتمل، وتظهر العملية كفشل أو تبقى في حالة غير نهائية. السبب كان أن مسار التنفيذ يشترط أحيانًا ظهور الحساب فورًا في بيانات أعضاء المجموعة قبل اعتبار النتيجة نجاحًا، رغم أن `groupAcceptInvite` أعاد معرف المجموعة بعد قبول الدعوة.

**الإصلاح الأول:** أصبح مسار العملية يحدّث `whatsapp_links` عند نجاح نتيجة الانضمام، ويكتب `joined=true` و`status='joined'` و`processing_status='completed'` و`last_status='success'`، إضافة إلى `last_operation_id` وبيانات الحساب المنضم.[11]

**الإصلاح الثاني:** إذا أعاد `groupAcceptInvite` معرف المجموعة بنجاح ولكن تأخرت بيانات `groupMetadata`، تُسجل النتيجة كـ`success` مع `membership_state='JOINED_UNVERIFIED'`، ويحفظ النظام سبب تأخر التحقق داخل `verification_evidence`. وبذلك لا يتحول الانضمام المقبول من WhatsApp إلى فشل زائف، مع الاحتفاظ بتمييز واضح بين القبول التشغيلي والتحقق التفصيلي.

| الحالة | حالة العملية | حالة الرابط |
|---|---|---|
| قبول الدعوة وظهور العضوية | `success` | `joined` و`completed` |
| قبول الدعوة وتأخر بيانات الأعضاء | `success` | `joined` و`completed` مع `JOINED_UNVERIFIED` |
| العضوية السابقة مؤكدة | `success` | `joined` مع `ALREADY_MEMBER` |
| رفض حقيقي أو رابط منتهٍ | `failed` أو `review` | `failed` أو `review` |
| طلب يحتاج موافقة مشرف | `review` | `review` |

### 3.7 ظهور فشل بسبب التحقق المتأخر من عضوية المجموعة

**المشكلة:** نتيجة الانضمام قد تكون ناجحة، لكن بيانات المشاركين في المجموعة لا تتحدث فورًا، أو يظهر الحساب بهوية LID بدل رقم الهاتف. الفحص القديم كان يعيد `retry` ثم قد يصل إلى `failed` إذا لم يجد الحساب في المحاولة السريعة.

**الإصلاح:** تم توسيع مطابقة هوية الحساب لتشمل رقم الهاتف وحقول LID والهويات المتوفرة من جلسة Baileys. كما أصبحت نتيجة قبول الدعوة نفسها كافية لتسجيل النجاح التشغيلي عندما يعيد WhatsApp معرف المجموعة، بينما يبقى التحقق التفصيلي دليلًا إضافيًا لا شرطًا يؤدي إلى فشل زائف.[12]

### 3.8 تجاوز الفاصل الزمني بين انضمامات الحساب الواحد

**المشكلة:** كان الإعداد يحدد نطاقًا مثل 120–360 ثانية، لكن الجدولة كانت تعتمد على تأخير العملية التالية داخل المهمة الحالية. إذا وُجدت عمليات أخرى للحساب نفسه أو أكثر من مهمة، فقد تكون Jobs جاهزة في الوقت نفسه. القفل كان يمنع التنفيذ المتزامن فقط، ثم يعيد Job بعد 5 ثوانٍ؛ لكنه لم يضمن مرور 120 ثانية منذ آخر انضمام فعلي.

**الإصلاح:** أُضيفت سياسة تبريد على مستوى الحساب. قبل بدء مرحلة الانضمام، يبحث النظام عن أحدث `join_completed_at` أو `join_started_at` لذلك الحساب عبر جميع العمليات، ثم يحسب الثواني المتبقية حتى الحد الأدنى. إذا لم ينتهِ الحد الأدنى، تُعاد جدولة العملية ولا يُستدعى WhatsApp.

كما أصبح قفل PostgreSQL على مستوى الحساب بدل أن يكون مرتبطًا بالعملية فقط، مع بقاء قفل Redis لحماية تدفق التشغيل. ويُسجل حدث `account_pacing_wait` متضمنًا مدة الانتظار والوقت المجدول وسبب التأجيل.[13]

| المعيار | السلوك الحالي |
|---|---|
| الحد الأدنى | لا يبدأ انضمام جديد قبل مرور قيمة `min_delay_seconds` |
| المجال العشوائي | يبقى التأخير الطبيعي بين الحد الأدنى والحد الأعلى |
| تعدد المهام | الفاصل يطبق عبر جميع مهام الحساب نفسه |
| القياس | يعتمد على آخر إكمال فعلي، وليس إنشاء المهمة أو إدخالها إلى Queue |
| التعارض | تُعاد جدولة العملية بدل تنفيذها بعد 5 ثوانٍ |
| العرض | سجل التشغيل يعرض الساعات والدقائق والثواني |

> الصورة المرفقة من WhatsApp تعرض الدقائق فقط، لذلك قد تبدو عمليتان في الدقيقة نفسها رغم أن بينهما ثوانٍ. بعد الإصلاح أصبح سجل اللوحة يعرض الثواني، وأصبح الخادم نفسه يفرض الحد الأدنى بدل الاعتماد على العرض البصري.

### 3.9 تجاهل الحد اليومي الذي يحدده المستخدم

**المشكلة:** كان الحد اليومي يظهر في الواجهة، لكن بعض مسارات إنشاء المهمة كانت تعود إلى القيمة الافتراضية أو لا تمرر القيمة إلى الخدمة، ما يجعل سلوك Direct Publish وLink Import مختلفًا عن الشاشة الرئيسية.

**الإصلاح:** تم توحيد قراءة وتمرير `dailyOperationLimit` و`daily_operation_limit` في الواجهات والمسارات الخلفية، مع قص القيمة إلى نطاق آمن من 1 إلى 5000 عملية لكل حساب. كما أصبحت نافذة المراجعة تعرض الحد الفعلي قبل تأكيد المهمة.[14] [15]

### 3.10 إيقاف المهمة تلقائيًا بسبب الحد اليومي رغم رغبة المستخدم في السماح

**المشكلة:** كان تجاوز الحد اليومي يمنع إنشاء المهمة مباشرة، حتى عندما يريد المستخدم تشغيلها مع تحمله مسؤولية معدل التشغيل.

**الإصلاح:** أضيف الإعداد `daily_limit_protection_enabled`. عند تعطيله لا يمنع النظام إنشاء المهمة بسبب quota، بينما تبقى حماية الحظر وForbidden وRate Limit والحساب غير المتصل فعالة. هذا فصل بين حماية quota الاختيارية وحماية الحساب الضرورية.[16]

### 3.11 استمرار التشغيل بعد تقييد الحساب أو حظره

**المشكلة:** إعادة المحاولة المستمرة بعد رسائل مثل `forbidden` أو `blocked` أو `banned` قد تزيد ضغط الحساب وتخفي السبب الحقيقي، كما يمكن أن تؤدي إلى استمرار Jobs للحساب المقيد.

**الإصلاح:** تم تصنيف أخطاء WhatsApp إلى أخطاء مؤقتة وأخطاء دائمة. عند ظهور تقييد أو Rate Limit أو حظر، ينتقل الحساب إلى `protected` أو `stopped`، وتُنقل عملياته المتبقية إلى `review`، وتُلغى Jobs الانتظار الخاصة به. لا تُعاد محاولة الرفض الصريح بلا نهاية.[17] [18]

### 3.12 تشغيل حساب متصل ظاهريًا لكنه غير جاهز فعليًا

**المشكلة:** كان الحساب قد يظهر `connected` في قاعدة البيانات، بينما جلسة WhatsApp نفسها غير جاهزة أو لا تملك إشارة حديثة. هذا يؤدي إلى فشل مبكر أو إلى مهمة تتراكم دون تنفيذ فعلي.

**الإصلاح:** تم الفصل بين `status='connected'` وبين جاهزية الجلسة من `WhatsAppManager.isReady`. كما أصبحت الواجهة تعرض الحساب غير المؤهل وتوفر إعادة فحص للحساب المتصل، ولا تسمح بإدخاله في مهمة جديدة قبل جاهزيته.[19]

### 3.13 عرض «يعمل» دون دليل حي من Worker

**المشكلة:** كان يمكن أن تعرض الواجهة حالة تشغيل اعتمادًا على حالة ثابتة أو وجود مهمة فقط، مع أن Worker أو Queue قد لا يكونان نشطين.

**الإصلاح:** أصبحت لوحة المراقبة تجمع بين حالة Queue، عدد Workers، نبضات الحساب، وآخر نشاط حديث. إذا غاب الدليل المركب تظهر الحالة «لا يوجد نشاط حالي — جارٍ التحقق» بدل إعطاء انطباع بأن التنفيذ مستمر.[20]

### 3.14 ظهور أسماء أحداث تقنية غير مفهومة

**المشكلة:** كانت الواجهة تعرض أسماء مثل `queue_outbox_created` و`queue_enqueued` دون شرح، ما يجعل المستخدم يظن أن ظهورها يعني نجاح الانضمام.

**الإصلاح:** تمت ترجمة الأحداث إلى رسائل عربية، مع عرض أسباب الرفض من `payload.error` و`payload.status` و`payload.reason`. أضيف أيضًا حدث `account_pacing_wait` إلى قاموس العرض حتى يعرف المستخدم أن التأجيل مقصود لحماية الفاصل.[21]

### 3.15 عدم ظهور الروابط المستوردة في لوحة أتمتة الانضمام

**المشكلة:** كان الرابط يُحفظ في جداول الاستيراد، لكن قد لا يملك بعد ذلك `discovered_link_id` يربطه بسجل `whatsapp_links` الذي تعتمد عليه لوحة الانضمام.

**الإصلاح:** أضيفت عملية مزامنة `syncImportedLinksToDashboard` عند تحميل اللوحة، وتقوم بإنشاء أو مطابقة سجل الرابط وربطه بسجل الاستيراد دون إنشاء تكرارات.[22]

### 3.16 التحديد الشامل للروابط لا يطابق الفلاتر

**المشكلة:** زر تحديد جميع الروابط كان يعتمد على الروابط الظاهرة في الصفحة الحالية، بينما المستخدم يتوقع تحديد جميع النتائج المطابقة للفلاتر والصفحات.

**الإصلاح:** أضيف Endpoint يعيد معرفات الروابط المطابقة للفلاتر الحالية، بما في ذلك البحث والحالة والمصدر والحساب والتاريخ وإظهار المكتملة، ثم تستخدم الواجهة هذه المعرفات بدل الاقتصار على الصفحة الحالية.[23]

### 3.17 تحديث الحالة في PostgreSQL بأنواع غير صحيحة

**المشكلة:** بعض استعلامات تحديث حالة الرابط كانت تمرر القيم إلى أعمدة نصية أو UUID دون تحويل صريح، ما قد يسبب فشل التحديث أو يجعل العملية تُكمل في WhatsApp دون أن تنعكس في اللوحة.

**الإصلاح:** تم إضافة تحويلات صريحة مثل `::varchar` و`::uuid` في استعلامات الحالة ومعرفات العمليات، مع تسجيل الخطأ وإيقاف المسار عند فشل التحديث بدل إخفائه.[24]

### 3.18 عدم احتساب المهمة مكتملة بعد انتهاء عملياتها

**المشكلة:** كانت المهمة قد تحتوي عمليات نهائية، لكن `completed_operations` أو حالة المهمة لا تُحدّث دائمًا بعد النجاح أو الفشل أو التخطي.

**الإصلاح:** تم توحيد `maybeCompleteTask` ليعيد احتساب العمليات النهائية، ثم يحول المهمة إلى `completed` عندما لا تبقى عمليات في حالات `pending` أو `processing` أو `retry` أو `paused`. يُستدعى ذلك بعد النجاح والفشل والاستعادة والإيقاف اليدوي.[25]

### 3.19 ضعف تفاصيل السجل لكل عملية

**المشكلة:** معرفة أن العملية فشلت لم تكن كافية لتحديد هل فشل الرابط أو الحساب أو Queue أو التحقق.

**الإصلاح:** أصبحت العملية تحفظ `current_stage` و`join_status` و`membership_state` و`verification_evidence` و`error_code` و`last_error` وحقول البداية والإكمال والـheartbeat. كما تعرض شاشة التفاصيل العمليات والأحداث المرتبطة بالرابط.[26]

---

## 4. حالات النجاح والفشل المعتمدة حاليًا

| الحالة | معناها | الإجراء |
|---|---|---|
| `queued` | العملية محفوظة وتنتظر دورها | لا يعني نجاح الانضمام |
| `processing` | Worker بدأ معالجة العملية | يجب انتظار نتيجة WhatsApp |
| `success` | العملية انتهت بنجاح | تزيد إحصائية المكتمل |
| `joined` | الرابط ارتبط بانضمام ناجح | يظهر في سجل الرابط كمكتمل |
| `already_joined` | الحساب كان منضمًا مسبقًا وتم تأكيد ذلك | نجاح تشغيلي مع `ALREADY_MEMBER` |
| `JOINED_UNVERIFIED` | WhatsApp قبل الدعوة، لكن بيانات الأعضاء متأخرة | نجاح، مع دليل تحقق مؤجل |
| `pending_approval` | الانضمام ينتظر موافقة مشرف | مراجعة، وليس نجاحًا نهائيًا |
| `retry` | خطأ مؤقت يمكن إعادة المحاولة | تأجيل محدود وفق الإعداد |
| `failed` | فشل نهائي في العملية | حفظ السبب وعدم اعتبارها مكتملة بنجاح |
| `review` | تحتاج تدخلًا بسبب حماية أو موافقة أو فشل مرحلة | إيقاف المسار حتى المراجعة |
| `account_pacing_wait` | العملية تأجلت لتطبيق الفاصل الأدنى للحساب | ليس فشلًا؛ ستعاد إلى Queue |

---

## 5. الملفات التي تحتوي الإصلاحات الأساسية

| الملف | ما يحتويه |
|---|---|
| [`GroupJoinerService.js`](https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/api/services/GroupJoinerService.js) | تنفيذ `groupAcceptInvite`، تصنيف الأخطاء، تأكيد العضوية، ومعالجة القبول مع تأخر بيانات الأعضاء |
| [`LinkImportService.js`](https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/api/services/LinkImportService.js) | إنشاء المهام، Outbox، Worker flow، حفظ الحالات، Recovery، وتطبيق تبريد الحساب |
| [`JoinScheduler.js`](https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/api/services/JoinScheduler.js) | حساب التأخير العشوائي وحساب الثواني المتبقية من الفاصل الأدنى |
| [`DelayEngine.js`](https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/api/services/DelayEngine.js) | تطبيع حدود التأخير وحساب وقت الجدولة |
| [`QueueManager.js`](https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/lib/QueueManager.js) | Queue `wa-link-imports` وعامل التنفيذ وقواعد الإضافة والإلغاء |
| [`TelegramController.js`](https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/api/controllers/TelegramController.js) | Dashboard والإحصاءات والتقارير ومسارات إنشاء المهام |
| [`DirectPublishSection.tsx`](https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/frontend/src/components/DirectPublishSection.tsx) | عدادات التشغيل وسجل المراحل وعرض الوقت بالثواني |
| [`WhatsAppJoinAutomationView.tsx`](https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/frontend/src/views/WhatsAppJoinAutomationView.tsx) | اللوحة الرئيسية والمراقبة والأحداث والفلترة ورسالة تبريد الحساب |
| [`LinkImportMigrations.js`](https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/database/LinkImportMigrations.js) | جداول المهام والعمليات والنتائج وحقول التحقق والـOutbox |
| [`TelegramMigrations.js`](https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/database/TelegramMigrations.js) | أعمدة `whatsapp_links` الخاصة بالحالة والانضمام والحسابات المنضمة |

---

## 6. الاختبارات المنفذة

تم تشغيل فحص الصياغة واختبارات Backend وبناء Frontend بعد إصلاحات النجاح والفاصل الزمني. شملت الاختبارات اختبارًا صريحًا لحساب الثواني المتبقية، واختبارًا لقبول الدعوة عند تأخر بيانات العضوية، واختبارات Queue والحماية والتصنيف والاستعادة.

| الفحص | النتيجة |
|---|---:|
| `node --check` للخدمات المعدلة | ناجح |
| اختبارات `GroupJoinerService` | ناجحة |
| اختبارات `LinkImportService` و`JoinScheduler` | ناجحة |
| Backend Jest كامل | 14 Test Suites ناجحة، 50 اختبارًا ناجحًا |
| Frontend production build | ناجح |
| `git diff --check` | ناجح |
| Git working tree بعد الرفع | نظيف |

> الاختبارات المحلية لا تتصل بحساب WhatsApp حقيقي ولا بقاعدة بيانات Railway؛ لذلك يلزم اختبار حي صغير بعد النشر.

---

## 7. تسلسل الـCommits المهمة

| Commit | الإصلاح |
|---|---|
| `68c23a0` | إضافة لوحة مراقبة حية لأتمتة الانضمام |
| `9a2cb80` | عدم عرض حالة تشغيل نشطة دون دليل حي |
| `55bd4ab` | إضافة Watchdog لاستعادة Queue المعلقة |
| `e1c4331` | جعل Worker قابلًا للتتبع بالأحداث |
| `00e52ae` | إعادة Jobs المعلقة بمعرفات فريدة |
| `80dc695` | إيقاف إعادة محاولة رفض WhatsApp الصريح بلا نهاية |
| `85b625a` | حفظ Worker ونتيجة الانضمام بشكل دائم |
| `16a7205` | مطابقة Payload المحفوظ مع حقول Worker |
| `8a2f9f6` | إيقاف الحساب عند تقييد WhatsApp |
| `6ed5c22` | عزل عمليات الحساب غير المتاح ونقلها للمراجعة |
| `a37b80b` | إعادة فحص الحسابات المتصلة غير الجاهزة |
| `b8cc0e6` | مزامنة الروابط المستوردة مع لوحة الانضمام |
| `d10f200` | إضافة مسار استيراد الروابط الآمن من Word والملفات المدعومة |
| `ea91e81` | تصحيح أنواع معاملات تحديث حالة الرابط |
| `a6580ab` | تقوية أتمتة WhatsApp في الإنتاج وإضافة Outbox وRecovery وVerification |
| `5f9f7df` | إضافة Audit Logs |
| `bf396e3` و`065a6c5` | تطبيق الحد اليومي المحفوظ عبر المسارات المختلفة |
| `1118fb5` | السماح للمستخدم بتحديد الحد اليومي |
| `b3a31a9` | جعل حماية الحد اليومي اختيارية |
| `8180505` | احتساب قبول WhatsApp الناجح كمكتمل عند تأخر بيانات العضوية |
| `f92dc15` | فرض الفاصل الأدنى على مستوى الحساب وعرض الثواني |
| `d0e46c3` | قياس الفاصل من آخر إكمال فعلي للانضمام |

---

## 8. خطوات التحقق بعد نشر Railway

بعد وصول الفرع `main` إلى Railway، يجب أولًا التأكد من أن Backend وFrontend يعملان من Commit `d0e46c3` أو أحدث. بعدها يُنصح بتشغيل رابط أو رابطين فقط قبل تشغيل قائمة كبيرة، لأن الهدف هو التأكد من أن الأحداث والحالات تظهر بالترتيب الصحيح.

| الخطوة | ما يجب التحقق منه |
|---|---|
| 1 | ظهور Worker وQueue كجاهزين مع Heartbeat حديث |
| 2 | ظهور `job_received` و`job_started` |
| 3 | ظهور `join_started` و`join_request_started` |
| 4 | ظهور `join_result_received` مع `success=true` عند القبول |
| 5 | ظهور `join_completed` ثم `operation_completed` |
| 6 | تحول العملية إلى `success` والرابط إلى `joined/completed` |
| 7 | زيادة عداد المكتمل في اللوحة بعد التحديث التلقائي |
| 8 | عند محاولة مبكرة، ظهور `account_pacing_wait` بدل تنفيذ سريع |
| 9 | فتح سجل التشغيل والتأكد من ظهور الثواني وليس الدقائق فقط |
| 10 | مقارنة `join_completed_at` بين عمليتين للحساب نفسه، ويجب ألا يقل الفرق عن `min_delay_seconds` |

للتأكد من الفاصل الحقيقي، لا تعتمد على وقت رسالة WhatsApp المعروض بالدقيقة فقط. يجب استخدام الحقول الزمنية المخزنة في `link_import_operations`، وبالأخص `join_completed_at`. الفاصل المحسوب هو:

```text
الفاصل بالثواني = وقت إكمال الانضمام الحالي − وقت إكمال الانضمام السابق للحساب نفسه
```

إذا ظهرت عملية ثانية قبل انتهاء الحد الأدنى، فابحث في الأحداث عن `account_pacing_wait` وعن قيمة `scheduledAt`. أما إذا لم يظهر هذا الحدث، فراجع أن النسخة المنشورة تحتوي Commit `d0e46c3` أو أحدث وأن Worker يعمل من نفس النسخة.

---

## 9. ما الذي لا يعالجه الكود تلقائيًا

لا يستطيع الكود إجبار WhatsApp على قبول رابط منتهي، أو تجاوز موافقة مشرف مجموعة، أو إزالة تقييد حساب، أو ضمان أن كل رابط صالح شكليًا قابل للانضمام فعليًا. كما أن تعطيل حماية الحد اليومي لا يلغي حمايات WhatsApp ولا يلغي التوقف عند Forbidden أو Rate Limit أو الحظر.

كذلك، العمليات القديمة التي تم تسجيلها قبل نشر إصلاح النجاح أو قبل إضافة تبريد الحساب قد لا تحتوي على كل الحقول الزمنية الجديدة. سيطبق الكود الجديد الحماية على Jobs التي تصل إلى Worker بعد النشر، لكن لا ينبغي إعادة تشغيل قائمة ضخمة دون مراجعة الحالات القديمة وتجنب إنشاء عمليات مكررة.

---

## 10. الخلاصة

المشاكل الأساسية لم تكن مشكلة واحدة في زر الانضمام، بل كانت سلسلة من نقاط التوافق بين نتيجة WhatsApp وقاعدة البيانات وQueue والواجهة. أهم إصلاحين نهائيين هما تسجيل قبول الدعوة كنجاح مكتمل حتى عند تأخر بيانات العضوية، وفرض الفاصل الأدنى على مستوى الحساب نفسه عبر جميع المهام، مع قياسه من آخر إكمال فعلي وتسجيل أي تأجيل وقائي.

بعد نشر Commit `d0e46c3`، يجب أن يظهر الانضمام المقبول في عداد **مكتملة**، وأن يظهر سجل الوقت بالثواني، وألا يبدأ انضمام جديد للحساب نفسه قبل انتهاء الحد الأدنى. قد يكون الفاصل أكبر من 360 ثانية عند وجود عملية طويلة أو تعارض في Queue؛ وهذا تأخير وقائي مقصود وليس تجاوزًا للإعداد.

---

## المراجع

[1]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/api/services/GroupJoinerService.js "GroupJoinerService — تنفيذ الانضمام الفعلي"

[2]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/api/services/LinkImportService.js "LinkImportService — دورة العملية والنتيجة"

[3]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/api/services/LinkUrlProcessingService.js "LinkUrlProcessingService — فحص وتطبيع الروابط"

[4]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/lib/QueueManager.js "QueueManager — Queue وWorkers"

[5]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/database/LinkImportMigrations.js "LinkImportMigrations — جداول Outbox والعمليات"

[6]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/55bd4ab "Watchdog لاستعادة Queue المعلقة"

[7]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/b030039 "استعادة العمليات المعلقة عند بدء الخادم"

[8]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/00e52ae "إعادة Jobs المعلقة بمعرفات فريدة"

[9]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/5d23f3b "إرسال عمليات الانضمام بأمان"

[10]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/16a7205 "مطابقة Payload المحفوظ مع Worker"

[11]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/8180505 "احتساب قبول WhatsApp الناجح كمكتمل"

[12]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/a6580ab "تقوية التحقق من عضوية WhatsApp"

[13]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/d0e46c3 "قياس الفاصل من آخر إكمال فعلي للانضمام"

[14]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/bf396e3 "تطبيق الحد اليومي المحفوظ"

[15]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/065a6c5 "تطبيق الحد اليومي عبر المسارات المختلفة"

[16]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/b3a31a9 "جعل حماية الحد اليومي اختيارية"

[17]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/8a2f9f6 "إيقاف الحساب عند تقييد WhatsApp"

[18]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/05e9a45 "إيقاف التشغيل بعد حظر WhatsApp"

[19]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/a37b80b "إعادة فحص الحسابات المتصلة"

[20]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/9a2cb80 "اشتراط دليل حي قبل عرض حالة التشغيل"

[21]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/6e5dc2b "تحسين إشعارات العملية"

[22]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/b8cc0e6 "مزامنة الروابط المستوردة"

[23]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/310b3ca "تحديد جميع الروابط المطابقة للفلاتر"

[24]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/ea91e81 "تصحيح معاملات تحديث الحالة"

[25]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/api/services/LinkImportService.js "maybeCompleteTask وحساب الحالة النهائية"

[26]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/frontend/src/views/WhatsAppJoinAutomationView.tsx "واجهة المراقبة والتفاصيل والأحداث"
