# WhatsApp Audit Center

## الهدف

تمت إضافة شاشة رسومية داخل لوحة التحكم لمراقبة سجلات التدقيق الفعلية الخاصة بقسم أتمتة الانضمام لروابط WhatsApp. الشاشة لا تولّد بيانات تجريبية؛ بل تقرأ من جدول `link_import_audit_logs` في PostgreSQL، وتعيد تحميل النتائج عند وصول حدث حي من الخادم.

## الوصول

يمكن فتح الشاشة من القائمة الجانبية عبر **سجل تدقيق واتساب** أو مباشرة عبر:

`/whatsapp-join-automation/audit`

يوجد أيضًا زر مباشر داخل رأس صفحة أتمتة الانضمام الرئيسية.

## ما تعرضه الشاشة

| المكوّن | الوصف |
|---|---|
| مؤشرات KPI | إجمالي السجلات، آخر 24 ساعة، آخر 7 أيام، وعدد المستخدمين المنفذين. |
| الرسم اليومي | أعمدة لعدد أحداث التدقيق خلال آخر 14 يومًا ضمن الفلاتر الحالية. |
| توزيع الإجراءات | نسب مرئية للإجراءات مثل الاستيراد وإنشاء المهمة والأرشفة والتصدير. |
| الفلاتر | بحث حر، الإجراء، نوع الكيان، تاريخ البداية، وتاريخ النهاية. |
| الخط الزمني | جدول صفحي يعرض الوقت والمستخدم والإجراء والكيان والمعرف ونطاق الحماية. |
| التفاصيل | Drawer يعرض IP وUser Agent وBefore State وAfter State بصيغة JSON منسقة. |
| التحديث الحي | حدث `whatsapp:audit_log_created` يحدّث الجدول والإحصاءات تلقائيًا. |
| التصدير | CSV مفلتر مع UTF-8 BOM، ويُسجل فعل التصدير في Audit نفسه. |

## API

| Method | Endpoint | الوظيفة |
|---|---|---|
| `GET` | `/api/whatsapp/join-automation/audit` | قائمة صفحية مع الفلاتر. |
| `GET` | `/api/whatsapp/join-automation/audit/stats` | مؤشرات KPI والتوزيعات اليومية. |
| `GET` | `/api/whatsapp/join-automation/audit/:id` | تفاصيل سجل واحد مع حارس ملكية. |
| `GET` | `/api/whatsapp/join-automation/audit/export` | تصدير CSV للسجلات المطابقة للفلاتر. |

المسارات محمية بالمصادقة الحالية. المستخدم العادي يرى السجلات التي يكون `actor_id` فيها مساويًا لمعرفه، بينما المشرف المصرح له يرى النطاق الإداري المتاح له وفق سياسة النظام.

## الحقول الحساسة

لا تعرض الشاشة Session Data أو Tokens أو Secrets أو API Hash. كما أن `recordAudit` ينقّي المفاتيح الحساسة قبل حفظ `before_state` و`after_state`، ويُستخدم نفس التنظيف في بيانات التفاصيل والتصدير.

## مخطط البيانات

يحتوي `link_import_audit_logs` على `actor_id`, `action`, `entity_type`, `entity_id`, `before_state`, `after_state`, `ip`, `user_agent`, و`created_at`. تمت إضافة فهرس زمني على المستخدم، وتُطبق Migration تلقائيًا عند إقلاع Backend.

## الإجراءات المسجلة حاليًا

| الإجراء | متى يُسجل |
|---|---|
| `IMPORT` | عند حفظ روابط مستوردة بنجاح. |
| `JOB_CREATE` | عند إنشاء مهمة WhatsApp جديدة. |
| `LINK_ARCHIVE` | عند أرشفة رابط من لوحة التحكم. |
| `EXPORT` | عند تصدير Audit Logs أو بعض تقارير القسم. |

## الاختبارات

تم التحقق من فحوصات Node syntax للملفات المعدلة، وبناء Frontend الإنتاجي، و`git diff --check`، واختبارات Backend الحالية دون Regression. لا يُعد ذلك اختبارًا حيًا لبيانات PostgreSQL أو Socket.IO في بيئة الإنتاج؛ يجب تنفيذ اختبار تكامل بعد النشر باستخدام مستخدمين منفصلين للتأكد من عزل الصلاحيات وتدفق التحديث الحي.

## تشغيل الإنتاج

بعد النشر أعد تشغيل Backend حتى تُنفذ Migration الخاصة بجدول Audit والفهارس. يجب التأكد من أن Socket.IO متصل بنفس خدمة Backend التي تخدم API، وأن المستخدم لديه Token صالح. إذا لم توجد سجلات، تعرض الشاشة حالة فارغة صحيحة بدل بيانات وهمية.
