# التقرير الشامل لتطوير أتمتة الانضمام لروابط WhatsApp

## ملف مراجعة تقني شامل ومخصص لـ Chat 4

| البند | التفاصيل |
|---|---|
| المشروع | `x781780889-jpg/whatsapp-dashboard-new` |
| القسم | أتمتة الانضمام لروابط WhatsApp داخل Dashboard |
| الفرع | `main` |
| آخر Commit | [`01fdebe`](https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/01fdebe) |
| تاريخ التقرير | 25 أغسطس 2026 |
| حالة الكود | تم رفع التعديلات إلى GitHub، ويجب إكمال التحقق الحي بعد نشرها على Railway |
| الغرض | شرح أسباب الفشل وتوثيق التعديل الكامل ومساعدة Chat 4 على مراجعة ما قد يكون ناقصًا |

> **تنبيه مهم:** هذا التقرير يشرح الكود، ومسار البيانات، واختبارات البيئة المحلية. لا يمكن تحديد سبب كل رابط فاشل في حساب WhatsApp الحقيقي من الصورة وحدها؛ تحديد السبب الفردي يتطلب قراءة سجل العملية نفسه من `error_code` و`last_error` و`result` و`link_import_events` بعد النشر.

---

## 1. الملخص التنفيذي

كانت المشكلة الظاهرة للمستخدم تتكون من أكثر من عرض في الوقت نفسه. بعض عمليات الانضمام كانت تنجح فعليًا داخل حساب WhatsApp، لكنها لا تظهر في Dashboard كمكتملة. وفي المقابل، كانت روابط أخرى تظهر في لوحة التحكم على أنها فاشلة، من دون أن يكون واضحًا هل فشل الرابط نفسه، أم الحساب، أم الاتصال، أم التحقق من العضوية، أم إرسال العملية إلى Queue.

كما ظهر خلل في الفاصل الزمني بين عمليتي انضمام للحساب نفسه. كان المستخدم يحدد مجالًا بين 120 و360 ثانية، لكن الاعتماد على وقت إنشاء المهمة أو إعادة Job بعد تعارض القفل لم يكن كافيًا لضمان الحد الأدنى بين **الانضمامات الفعلية**. لذلك أصبحت هناك حاجة إلى إعادة بناء المسار من مرحلة إنشاء المهمة حتى مرحلة تنفيذ `groupAcceptInvite` وتسجيل النتيجة.

التطوير المنفذ نقل النظام من جدولة عملية مفردة داخل مهمة عامة إلى نظام دائم يعتمد على Backend وPostgreSQL وOutbox وBullMQ وWorker وقفل على مستوى الحساب. أصبح لكل حساب داخل المهمة دورة زمنية مدتها 60 دقيقة وبحد أقصى 30 رابطًا. تُوزع الروابط زمنيًا ولا تُرسل دفعة واحدة، وتُحتسب كل محاولة نهائية ضمن العداد سواء انتهت بـ`JOINED` أو `JOIN_REQUEST_SENT` أو `FAILED`.

---

## 2. ما الذي يعنيه ظهور الرابط «فاشلًا»؟

ظهور الرابط كفاشل في اللوحة لا يعني دائمًا أن WhatsApp رفض عملية الانضمام بنفس السبب. كلمة «فشل» في الواجهة هي نتيجة نهائية مختصرة، بينما السبب الحقيقي يجب قراءته من عدة حقول مرتبطة بالعملية.

| الحقل | وظيفته | لماذا هو مهم؟ |
|---|---|---|
| `operation.status` | الحالة النهائية للعملية | يحدد هل انتهت العملية بنجاح أو فشل أو مراجعة |
| `join_status` | نتيجة مرحلة الانضمام | يميز النجاح والطلب المعلق والفشل |
| `membership_state` | حالة عضوية الحساب | مثل `ALREADY_MEMBER` أو `JOINED_UNVERIFIED` |
| `error_code` | رمز تقني مختصر | يسمح بتجميع أسباب الفشل بدقة |
| `last_error` | شرح نصي للخطأ | يعرض سبب الفشل للمستخدم أو للمراجع |
| `result` | النتيجة الأصلية بصيغة JSON | تحفظ `status` و`success` و`error` و`groupId` ودليل التحقق |
| `join_started_at` | وقت بدء طلب الانضمام | يثبت وقت إرسال محاولة الانضمام |
| `join_completed_at` | وقت اكتمال مرحلة الانضمام | يستخدم لحساب الفاصل الحقيقي بين المحاولات |
| `link_import_events` | سجل الأحداث | يوضح هل وصلت العملية إلى Worker وWhatsApp أم توقفت قبل ذلك |

### 2.1 الأسباب الحقيقية المحتملة للفشل

#### أ. الرابط غير صالح أو منتهي

يحدث ذلك عندما لا يستطيع النظام استخراج Invite Code من الرابط، أو عندما يرد WhatsApp بأن الدعوة غير صالحة أو منتهية أو غير موجودة. تشمل هذه الفئة حالات مثل `invalid_link` و`LINK_INVALID_OR_EXPIRED` و`bad-request` و`not-found` و`404`.

صلاحية الرابط الشكلية لا تضمن أن الدعوة ما زالت فعالة. ففحص الصيغة يتأكد من أن النص يشبه `https://chat.whatsapp.com/...`، لكنه لا يستطيع ضمان أن المجموعة موجودة أو أن الرابط لم يُلغَ أو أن الحساب مسموح له باستخدامه.

#### ب. الحساب غير متصل أو جلسة WhatsApp غير جاهزة

إذا كان الحساب في قاعدة البيانات يحمل `status='connected'` لكن جلسة WhatsApp نفسها لم تصبح جاهزة فعليًا، فلن يكون من الآمن إرسال طلب الانضمام. لذلك يفحص النظام `WhatsAppManager.isReady(accountId)` بالإضافة إلى حالة الحساب في قاعدة البيانات.

الحالات المرتبطة بهذا السبب تشمل `account_offline` أو `ACCOUNT_UNAVAILABLE` أو رسالة تفيد بأن الجلسة غير متصلة أو غير جاهزة. لا ينبغي اعتبار هذا رفضًا للرابط؛ إنه فشل في جاهزية الحساب أو الاتصال ويجب أن يعالج بإعادة الاتصال أو الانتظار.

#### ج. تقييد أو حظر الحساب

إذا أعاد WhatsApp رسالة تحتوي على `forbidden` أو `blocked` أو `banned` أو `unauthorized` أو `permission` أو `account reachout restricted`، فهذه ليست مشكلة رابط منفرد فقط. النظام يصنفها كحالة حماية للحساب، ويكتب `ACCOUNT_RESTRICTED` أو `ACCOUNT_RATE_LIMITED`، ويحوّل الحساب إلى حالة محمية أو موقوفة، وينقل العمليات المتبقية إلى `review` بدل تكرار الطلبات.

الهدف من ذلك هو عدم الاستمرار في إعادة محاولة حساب عليه إشارة تقييد، لأن التكرار قد يزيد الضغط على الحساب ويخفي السبب الحقيقي. إعادة تفعيل الحساب لا تتم آليًا؛ يجب أن يبقى تحت مراجعة المستخدم وبعد التأكد من أن جلسة WhatsApp جاهزة.

#### د. تجاوز معدل الاستخدام أو Rate Limit

إذا أعاد WhatsApp `429` أو `too many requests` أو `throttled` أو `temporarily restricted`، يتعامل النظام معه كحالة حماية. العملية الحالية لا تستمر كأنها فشل عادي، بل تُسجل إشارة التقييد، ويُوقف الحساب عن بقية عمليات المهمة، وتُنقل العمليات المتأثرة إلى المراجعة.

الفاصل الزمني 120–360 ثانية هو سياسة جدولة داخل النظام، لكنه لا يمثل ضمانًا بأن WhatsApp لن يفرض حماية إضافية. سياسات WhatsApp الخارجية لا يمكن تجاوزها برمجيًا، ولذلك يبقى التوقف عند Rate Limit مقصودًا.

#### هـ. خطأ اتصال أو Timeout

تشمل هذه الفئة انقطاع الشبكة، تأخر جلسة Baileys، توقف Socket، أو انتهاء مهلة تنفيذ طلب الانضمام. تصنف هذه الحالات عادة إلى `retry` إذا كان الخطأ مؤقتًا وقابلًا للإعادة، مع احترام `max_retries` و`retry_backoff_seconds`.

لا تُحتسب إعادة المحاولة المؤقتة كعملية دورة جديدة في كل مرة؛ بل تبقى العملية نفسها حتى تصل إلى نتيجة نهائية. عند الوصول إلى الحد الأقصى للإعادات أو ثبوت أن الخطأ غير قابل للإصلاح، تنتقل العملية إلى `failed` أو `review` حسب التصنيف.

#### و. انتظار موافقة مشرف المجموعة

قد يقبل WhatsApp إرسال طلب الانضمام لكنه لا يضيف الحساب فورًا لأن المجموعة تتطلب موافقة مشرف. هذه الحالة لا ينبغي أن توقف الدورة ولا أن تجعل Worker ينتظر الموافقة.

بعد التحديث، تُحفظ النتيجة كـ`JOIN_REQUEST_SENT`، وتُحتسب `Processed +1`، وتنتقل الدورة إلى الرابط التالي بعد الفاصل المحدد. لا يعاد إرسال الطلب نفسه داخل المهمة نفسها بسبب مفتاح العملية الفريد.

في بعض المسارات الداخلية قد تكون `operation.status='success'` لأن المعالجة انتهت تشغيليًا، بينما يكون `join_status='pending_approval'` و`error_code='JOIN_REQUEST_SENT'`. لذلك يجب قراءة `join_status` و`result` مع `operation.status` بدل الاعتماد على حقل واحد.

#### ز. فشل التحقق التفصيلي رغم قبول الدعوة

هذا كان سببًا رئيسيًا لظهور عمليات فاشلة رغم أن الحساب انضم فعليًا. كان `groupAcceptInvite` يعيد معرف المجموعة، ثم يتأخر تحديث `groupMetadata` أو تظهر هوية الحساب بصيغة LID بدل رقم الهاتف. إذا اشترط النظام ظهور الحساب فورًا في قائمة المشاركين، كان يحول القبول الحقيقي إلى فشل زائف.

بعد الإصلاح، أصبحت نتيجة قبول الدعوة من WhatsApp دليل نجاح تشغيلي. إذا تعذر التحقق التفصيلي مباشرة، تسجل العملية كنجاح مع `membership_state='JOINED_UNVERIFIED'` ويُحفظ سبب التأخير في `verification_evidence`. أما `ALREADY_MEMBER` فتسجل نجاحًا إذا أثبت التحقق أن الحساب كان منضمًا مسبقًا.

#### ح. فشل مرحلة النشر أو الخروج في Workflow المتدرج

إذا كانت المهمة تستخدم `workflow_mode='staged'`، فقد ينجح الانضمام ثم يفشل نشر الإعلان أو الخروج من المجموعة. في هذه الحالة لا يصح وصف مرحلة الانضمام نفسها بأنها فشلت. يجب قراءة `join_status` و`publish_status` و`leave_status` منفصلة.

العملية النهائية قد تنتقل إلى `review` بسبب فشل النشر أو الخروج، مع بقاء `join_status='success'`. هذا التفصيل مهم حتى لا يظن المراجع أن الحساب لم ينضم بينما المشكلة حدثت بعد الانضمام.

#### ط. انتهاء Lease أو توقف Worker أثناء التنفيذ

إذا بدأ Worker العملية ثم توقف قبل تسجيل النتيجة، قد تبقى العملية في `processing`. يقوم Recovery Watchdog بفحص `lease_expires_at` و`heartbeat_at`، ويحاول التأكد من العضوية الحالية قبل إعادة العملية إلى `retry`.

إذا وجد أن الحساب أصبح عضوًا بالفعل، تُستعاد العملية كنجاح `ALREADY_MEMBER` بدل إرسال طلب انضمام جديد. هذا يمنع إعادة تنفيذ الرابط بعد Crash أو Restart.

### 2.2 ما ليس فشلًا حقيقيًا

| الحالة الظاهرة | التفسير الصحيح |
|---|---|
| `queued` | العملية محفوظة وتنتظر دورها، ولم تُرسل إلى WhatsApp بعد |
| `queue_outbox_created` | تم حفظ حدث Outbox فقط، ولا يثبت نجاح الانضمام |
| `queue_enqueued` | أُرسلت العملية إلى Queue، ولا يثبت أن Worker نفذها |
| `account_pacing_wait` | تأجيل مقصود لتطبيق الحد الأدنى للفاصل |
| `JOINED_UNVERIFIED` | قبول فعلي من WhatsApp مع تأخر التحقق التفصيلي |
| `JOIN_REQUEST_SENT` | أُرسل الطلب، ولا ينتظر النظام موافقة المشرف لإكمال الدورة |
| `RESTING` | استراحة صحيحة بعد دورة الحساب، وليست حالة توقف نهائي |
| `retry` | خطأ مؤقت أو استعادة بعد توقف، وليس فشلًا نهائيًا بعد |

---

## 3. تحليل الصورة التي أرسلها المستخدم

الصورة المرفقة تعرض إشعارات داخل تطبيق WhatsApp، وتعرض أوقاتًا بالدقائق فقط. لذلك يمكن أن تبدو عمليتان داخل الدقيقة نفسها، بينما يكون بينهما مثلًا 40 أو 59 ثانية. لا يمكن من الصورة وحدها إثبات الفارق بالثواني أو ربط كل إشعار بسجل قاعدة البيانات.

المرجع الصحيح للتحليل هو `join_completed_at` أو على الأقل `join_started_at` داخل `link_import_operations`. الفاصل المطلوب حسابه هو:

```text
الفاصل الحقيقي = وقت العملية الحالية − وقت العملية السابقة للحساب نفسه
```

أما وقت رسالة WhatsApp فهو دليل بصري تقريبي فقط. بعد التحديث أصبحت لوحة التحكم تعرض الثواني في السجل، وأصبح Backend يفحص الفاصل قبل استدعاء `groupAcceptInvite`، لذلك لا يعتمد النظام على دقة عرض الهاتف.

---

## 4. دورة التنفيذ بعد التحديث

أصبح المسار العام للعملية كما يلي:

```text
اختيار الحسابات والروابط
        ↓
فحص وتطبيع رابط الدعوة
        ↓
حفظ whatsapp_links و link_import_links
        ↓
إنشاء link_import_task
        ↓
إنشاء link_import_operations
        ↓
إنشاء دورة لكل حساب
        ↓
تخصيص 30 عملية كحد أقصى للدورة
        ↓
جدولة أول عملية لكل حساب
        ↓
link_import_outbox
        ↓
Queue: wa-link-imports
        ↓
Worker مستقل عن Dashboard
        ↓
فحص الحساب والقفل والفاصل
        ↓
groupAcceptInvite داخل جلسة WhatsApp
        ↓
تصنيف JOINED أو JOIN_REQUEST_SENT أو FAILED
        ↓
تحديث العملية والدورة والرابط
        ↓
جدولة العملية التالية بعد النتيجة الحالية
        ↓
30/30 أو انتهاء نافذة الساعة
        ↓
RESTING
        ↓
Job مؤجل للدورة التالية
        ↓
NEXT_CYCLE
```

| المرحلة | المكون | الوظيفة |
|---|---|---|
| الفحص | `LinkUrlProcessingService` | استخراج وتطبيع Invite Code والتحقق من صيغة الرابط |
| التخزين | `whatsapp_links` | سجل الرابط الذي تعرضه اللوحة |
| التخزين الوسيط | `link_import_links` | ربط الرابط بالمهمة والمستخدم |
| المهمة | `link_import_tasks` | الإعدادات العامة والحالة والإجمالي |
| العملية | `link_import_operations` | محاولة حساب × رابط ونتيجتها التفصيلية |
| الدورة | `link_import_cycles` | عدادات الحساب وفترة الساعة والاستراحة |
| Outbox | `link_import_outbox` | ضمان عدم فقدان طلب الإرسال إلى Queue |
| Queue | `wa-link-imports` | نقل العمل إلى Worker دائم |
| التنفيذ | `GroupJoinerService` | استدعاء WhatsApp الحقيقي وتصنيف النتيجة |
| العرض | `WhatsAppJoinAutomationView` | عرض حالة الحساب والدورة والنتيجة |

---

## 5. قواعد الدورة الزمنية لكل حساب

### 5.1 القيم الأساسية

| الإعداد | القيمة |
|---|---:|
| مدة الدورة | 60 دقيقة |
| الحد الأقصى | 30 رابطًا لكل حساب |
| الحد الأدنى للفاصل | من إعداد المستخدم، مثل 120 ثانية |
| الحد الأعلى للفاصل | من إعداد المستخدم، مثل 360 ثانية |
| الاستئناف | مفعل افتراضيًا |
| التشغيل | Backend وWorker، وليس Dashboard |
| الحد النهائي للدورة | لا يتجاوز 30 عملية نهائية |

### 5.2 معنى Processed

العداد يعتمد على عدد الروابط التي وصلت إلى نتيجة نهائية، وليس على عدد النجاحات فقط. المثال التالي يساوي دورة مكتملة:

| النتيجة | العدد |
|---|---:|
| `JOINED` | 10 |
| `JOIN_REQUEST_SENT` | 8 |
| `FAILED` | 12 |
| **Processed** | **30** |

إذا كانت النتيجة فشلًا مؤقتًا وما زالت في `retry`، فلا تُحتسب العملية مرة ثانية عند كل إعادة. تُحتسب عند وصول العملية إلى نتيجتها النهائية، ويضمن الحقل `cycle_counted_at` عدم احتسابها مرتين.

### 5.3 انتهاء الدورة

عند وصول `processed_count` إلى 30، أو عند انتهاء نافذة الساعة، يقوم النظام بإلغاء تخصيص العمليات المعلقة من الدورة الحالية، ويحفظ الدورة كـ`RESTING`، ويحفظ `next_cycle_at`. لا يُسمح بجدولة العملية رقم 31 في الدورة نفسها.

إذا بقي أقل من 30 رابطًا، مثل 12 رابطًا فقط، تُسجل الدورة كـ`12/30` ثم تدخل الاستراحة حتى موعد الدورة التالية. وعند عدم وجود روابط متبقية بعد الدورة التالية، تُغلق المهمة نهائيًا بدل إنشاء دورات فارغة بلا نهاية.

### 5.4 التوزيع الزمني

بعد إنهاء العملية الحالية وتسجيل نتيجتها، يحسب النظام التأخير التالي. لا يعتمد على وقت إنشاء المهمة ولا على وقت إدخال الرابط إلى Queue. يمرر النظام عدد العمليات المتبقية ونهاية الدورة إلى `JoinScheduler`، فيستخدم التأخير العشوائي داخل حدود الإعداد مع تقليل سقفه عند الحاجة حتى يتوزع العمل على الساعة.

إذا كان الحد الأدنى 120 ثانية وبقي 29 رابطًا في 3600 ثانية، يصبح السقف العادل التقريبي:

```text
3600 ÷ 29 ≈ 124 ثانية
```

لذلك يمكن أن يكون التأخير التالي بين 120 و124 ثانية في تلك الحالة، بدل اختيار 360 ثانية تجعل بقية الروابط تتجاوز نافذة الدورة. وإذا لم يكن هناك وقت كافٍ لتنفيذ كل الروابط بالحد الأدنى، لا يخفض النظام الحد الأدنى؛ بل يترك الروابط المتبقية للدورة التالية.

---

## 6. حالات النظام State Machine

```text
IDLE
  ↓
RUNNING
  ↓
PROCESSING
  ↓
JOINED / JOIN_REQUEST_SENT / FAILED
  ↓
WAITING_NEXT
  ↓
PROCESSING
  ↓
...
  ↓
30/30 أو نهاية النافذة
  ↓
RESTING
  ↓
NEXT_CYCLE
  ↓
RUNNING
```

| الحالة | المعنى |
|---|---|
| `pending` | العملية موجودة وتنتظر الجدولة |
| `processing` | Worker يعالج العملية حاليًا |
| `success` | العملية انتهت تشغيليًا بنجاح |
| `failed` | فشل نهائي مع سبب محفوظ |
| `review` | تحتاج تدخلًا، مثل تقييد الحساب أو فشل مرحلة لاحقة |
| `retry` | خطأ مؤقت أو عملية مستعادة |
| `paused` | المهمة متوقفة مؤقتًا |
| `skipped` | تم تجاوزها بسبب إيقاف المهمة أو حماية الحساب |
| `RUNNING` | حالة دورة الحساب الحالية |
| `RESTING` | استراحة الدورة قبل الموعد التالي |
| `ACCOUNT_DISCONNECTED` | الحساب غير متصل أو غير جاهز |
| `SERVER_OFFLINE` | لا يوجد دليل حي على Worker أو Queue |
| `STOPPED` | إيقاف نهائي مقصود، وليس استراحة دورة |

من المهم ألا تعرض الواجهة `STOPPED` عندما تكون الدورة فقط في فترة استراحة. العرض الصحيح هو `RESTING` مع موعد الدورة التالية.

---

## 7. التعديلات التي تمت في Backend

### 7.1 `LinkImportService.js`

هذا هو الملف الرئيسي الذي يحتوي على إنشاء المهمة، تخصيص العمليات، جدولة الرابط التالي، تنفيذ العملية، حفظ النتيجة، الاستعادة، وحساب حالة المهمة.

تمت إضافة وظائف الدورة التالية:

| الوظيفة | العمل |
|---|---|
| `getCycleSettings` | قراءة إعدادات الدورة من المهمة |
| `ensureAccountCycle` | إنشاء أو استرجاع دورة الحساب الحالية |
| `assignOperationsToCycle` | تخصيص حتى 30 عملية للدورة |
| `scheduleAccountOperation` | جدولة عملية واحدة لحساب واحد |
| `scheduleNextOperation` | جدولة الحساب المتأثر أو جميع حسابات المهمة عند البدء |
| `maybeCloseCycle` | تحويل الدورة إلى `RESTING` عند 30 أو انتهاء النافذة |
| `advanceCycle` | بدء الدورة التالية بعد موعد الاستراحة |
| `countCycleOperation` | زيادة عداد الدورة مرة واحدة فقط لكل عملية |
| `enqueueCycleWakeup` | إنشاء Job مؤجل لإيقاظ الدورة التالية |

تم تعديل `createTask` ليحفظ `cycle_limit=30` و`cycle_duration_minutes=60` و`auto_resume=true`، ثم يبدأ أول عملية لكل حساب بدل وضع عملية واحدة عامة فقط.

### 7.2 `GroupJoinerService.js`

ينفذ الملف طلب الانضمام الحقيقي عبر:

```javascript
groupAcceptInvite(inviteCode)
```

ثم يحاول التحقق من العضوية عبر `groupMetadata` وبيانات هوية الحساب، بما فيها رقم الهاتف وحقول LID المتاحة. إذا أعاد WhatsApp معرف المجموعة وقبل الدعوة، تسجل النتيجة نجاحًا تشغيليًا حتى لو تأخر ظهور الحساب في قائمة المشاركين.

### 7.3 `JoinScheduler.js`

تم توسيع الجدولة لتقبل:

```javascript
remainingOperations
cycleEndAt
```

وتستخدم هذه القيم لتقليل سقف التأخير العشوائي عند الحاجة، مع الحفاظ على الحد الأدنى. كما بقيت وظيفة حساب الفاصل من آخر انضمام فعلي، بحيث لا يبدأ الحساب عملية جديدة قبل مرور الحد الأدنى.

### 7.4 `JoinCyclePolicy.js`

تمت إضافة وحدة مستقلة قابلة للاختبار لتوحيد:

- الحد الافتراضي 30.
- مدة الدورة الافتراضية 60 دقيقة.
- معرفة انتهاء نافذة الدورة.
- معرفة متى يجب الانتقال إلى الاستراحة.
- حساب الخانات المتبقية.
- تصنيف نتيجة WhatsApp إلى `JOINED` أو `JOIN_REQUEST_SENT` أو `FAILED`.

### 7.5 `QueueManager.js`

تمت إضافة Job باسم:

```text
advance_link_import_cycle
```

ويستخدم لإيقاظ الدورة التالية بعد `next_cycle_at`. كما أصبح Worker الخاص بعمليات الروابط يعمل بتزامن افتراضي قدره 3 حسابات، مع قفل الخدمة على مستوى الحساب حتى لا تتداخل عمليتان للحساب نفسه.

يمكن ضبط التزامن عبر:

```text
LINK_IMPORT_CONCURRENCY
```

مع بقاء القفل لكل حساب مستقلًا.

### 7.6 `backend/index.js`

تم تسجيل Handler جديد لـ`advance_link_import_cycle`، بحيث ينفذ `LinkImportService.advanceCycle(job.data)` داخل Worker الخلفي. بذلك لا يعتمد الانتقال إلى الدورة التالية على فتح Dashboard.

---

## 8. التعديلات التي تمت في قاعدة البيانات

تم تحديث `LinkImportMigrations.js` ليضيف جداول وحقولًا جديدة بطريقة آمنة على قواعد البيانات الموجودة.

### 8.1 جدول `link_import_cycles`

| العمود | الوظيفة |
|---|---|
| `id` | معرف الدورة |
| `task_id` | المهمة التي أنشأت الدورة |
| `user_id` | مالك المهمة |
| `account_id` | حساب WhatsApp الخاص بالدورة |
| `cycle_number` | رقم الدورة للحساب داخل المهمة |
| `cycle_start` | وقت بداية الدورة |
| `cycle_end` | وقت نهاية نافذة الساعة |
| `processed_count` | عدد العمليات المحتسبة |
| `success_count` | عدد `JOINED` |
| `request_count` | عدد `JOIN_REQUEST_SENT` |
| `failed_count` | عدد `FAILED` |
| `remaining_count` | عدد العمليات المتبقية |
| `current_operation_id` | العملية الحالية أو المجدولة |
| `current_link_id` | الرابط الحالي أو المجدول |
| `last_result` | آخر تصنيف للنتيجة |
| `status` | `RUNNING` أو `RESTING` أو `COMPLETED` |
| `next_cycle_at` | موعد استئناف الدورة التالية |
| `created_at` | وقت الإنشاء |
| `updated_at` | آخر تحديث |

### 8.2 حقول `link_import_operations`

تمت إضافة أو مزامنة الحقول التالية:

| الحقل | الاستخدام |
|---|---|
| `cycle_id` | ربط العملية بالدورة |
| `cycle_counted_at` | منع احتساب العملية مرتين |
| `next_run_at` | وقت التشغيل القادم للعمليات المعلقة |
| `join_started_at` | وقت بدء طلب WhatsApp |
| `join_completed_at` | وقت اكتمال الانضمام |
| `error_code` | تصنيف سبب الفشل |
| `last_error` | النص التفصيلي للخطأ |
| `result` | JSON لنتيجة WhatsApp |
| `heartbeat_at` | آخر إشارة من Worker |
| `lease_expires_at` | نهاية Lease العملية |
| `recovery_count` | عدد مرات الاستعادة |

### 8.3 حقول المهمة والإعدادات

تمت إضافة:

```text
cycle_limit = 30
cycle_duration_minutes = 60
auto_resume = true
```

إلى `link_import_tasks` و`join_automation_settings`. القيم الإلزامية للدورة مثبتة على 30 و60، بينما يبقى مجال الفاصل بين العمليات من إعدادات المستخدم ضمن حدود النظام.

---

## 9. منع التكرار والقفل

منع التكرار يعتمد على عدة طبقات، وليس على زر في الواجهة فقط.

أولًا، يوجد مفتاح فريد للعملية داخل المهمة يعتمد على المستخدم والمهمة والحساب والرابط. هذا يمنع إنشاء عمليتين متطابقتين لعلاقة الحساب بالرابط نفسه داخل المهمة.

ثانيًا، يوجد `cycle_counted_at` حتى لا ترفع إعادة المحاولة أو Recovery عداد الدورة مرتين.

ثالثًا، يتم ربط كل عملية بالدورة قبل تشغيلها. عند الوصول إلى 30 عملية، لا تعود وظيفة الجدولة إلى العملية التالية، بل تحول الدورة إلى `RESTING` وتترك الرابط رقم 31 للدورة التالية.

رابعًا، يوجد قفل PostgreSQL وAdvisory Lock وقفل الحساب في مسار التنفيذ. إذا وصلت Jobs مكررة للحساب نفسه، لا تنفذ عمليتان داخل WhatsApp في الوقت نفسه؛ تعاد العملية إلى Queue بتأخير بدلاً من تجاوز القفل.

خامسًا، عند حدوث Restart أو Timeout، يحاول Recovery التأكد من أن الحساب انضم فعليًا قبل إعادة الطلب. إذا تأكدت العضوية، تُستعاد العملية كـ`ALREADY_MEMBER` ولا يعاد إرسال دعوة جديدة.

---

## 10. التشغيل في الخلفية والاستمرارية

Dashboard ليس المحرك. هو واجهة مراقبة وتحكم فقط. التنفيذ الحقيقي محفوظ في Backend وQueue وقاعدة البيانات.

| إجراء المستخدم | السلوك المتوقع بعد التحديث |
|---|---|
| إغلاق Dashboard | تستمر Queue وWorker في تنفيذ المهمة |
| تحديث الصفحة | تسترجع الواجهة الحالة من API من دون تصفير العداد |
| إغلاق المتصفح | لا يوقف المهمة |
| إغلاق تطبيق الهاتف | لا يؤثر على Worker الموجود في الخادم |
| انقطاع إنترنت جهاز المستخدم | لا يوقف الخادم أو الحساب المتصل |
| إعادة تشغيل Worker | يستعيد العمليات والدورات من PostgreSQL |
| إعادة تشغيل السيرفر | يعاد بناء Jobs المعلقة وتستعاد الدورات المستحقة |
| Deployment | تحفظ الحالة الدائمة، ولا تبدأ دورة عشوائية إذا كانت البيانات موجودة |

Recovery يفحص العمليات ذات Lease المنتهية، والدورات التي انتهت نافذتها، والعمليات المعلقة التي لم تصل إلى Queue. الدورة ذات الحالة `RESTING` تحفظ `next_cycle_at`، وعند وصول الموعد ينفذ Job `advance_link_import_cycle`.

---

## 11. تحديثات Dashboard

### 11.1 بطاقة الحساب

تمت إضافة معلومات الدورة إلى بطاقة كل حساب:

```text
حساب WhatsApp
الحالة: متصل ويعمل
الدورة: 17 / 30
JOINED: 10
REQUEST: 5
FAILED: 2
المتبقي: 13
الحالة: RUNNING
العملية القادمة: الوقت المحفوظ في next_run_at
```

وعند الاستراحة:

```text
RESTING · استراحة
30 / 30
الدورة التالية: next_cycle_at
سيتم الاستئناف تلقائيًا
```

### 11.2 سجل الأحداث

تمت ترجمة الأحداث الجديدة إلى رسائل واضحة، ومنها:

| الحدث | الرسالة المقصودة |
|---|---|
| `cycle_started` | بدأت دورة جديدة للحساب |
| `cycle_resting` | انتهت دورة الحساب ودخلت فترة الاستراحة |
| `join_request_sent` | تم إرسال طلب الانضمام وتسجيله ضمن الدورة |
| `account_pacing_wait` | تم تأجيل العملية لحماية الفاصل الزمني |
| `operation_completed` | اكتملت عملية الانضمام |
| `operation_failed` | فشلت العملية مع عرض السبب |

### 11.3 إعدادات القسم

أصبحت اللوحة تعرض بوضوح:

| الإعداد | العرض |
|---|---|
| حد الدورة | 30 رابطًا |
| مدة الدورة | 60 دقيقة |
| الجدولة العشوائية | مفعلة عبر الفاصل المحفوظ |
| التشغيل بالخلفية | مفعّل عبر Backend وQueue |
| الاستئناف التلقائي | مفعّل افتراضيًا |
| حفظ الحالة | مفعّل عبر PostgreSQL |
| منع التكرار | مفعّل عبر القيود والقفل والـidempotency |

---

## 12. الاختبارات المنفذة

تم تشغيل الفحوص بعد آخر تعديلات الدورة والاستعادة والواجهة.

| الفحص | النتيجة |
|---|---:|
| `node --check` لملفات Backend المعدلة | ناجح |
| اختبار `JoinCyclePolicy` | ناجح |
| اختبار الحد 30 رابطًا | ناجح |
| اختبار انتهاء نافذة الساعة | ناجح |
| اختبار تصنيف `JOINED` | ناجح |
| اختبار تصنيف `JOIN_REQUEST_SENT` | ناجح |
| اختبار تصنيف `FAILED` | ناجح |
| اختبار التوزيع العشوائي داخل الزمن المتبقي | ناجح |
| اختبار الفاصل الأدنى بين الانضمامات | ناجح |
| Backend Jest | 15 Test Suites و54 اختبارًا ناجحًا |
| Frontend Production Build | ناجح |
| `git diff --check` | ناجح |
| Git Push | تم إلى `origin/main` |

### 12.1 ما لم يتم اختباره حيًا بعد

الاختبارات المحلية لا تنفذ دعوة حقيقية داخل حساب WhatsApp ولا تحاكي ساعة كاملة على جلسة Railway حقيقية. لذلك يجب إجراء اختبار حي صغير بعد النشر يتضمن أكثر من 30 رابطًا وحسابًا متصلًا، ثم اختبار إغلاق Dashboard وإعادة تشغيل الخادم أثناء دورة مثل `17/30`.

---

## 13. قائمة الاختبار الحي بعد نشر Railway

| الرقم | الاختبار | النتيجة المطلوبة |
|---:|---|---|
| 1 | تشغيل حساب واحد مع 3 إلى 5 روابط | يبدأ أول رابط من Worker ويظهر `job_started` |
| 2 | فحص نتيجة قبول الدعوة | تظهر `join_result_received` و`success=true` عند النجاح |
| 3 | فحص الرابط الناجح | يتحول الرابط إلى `joined` و`completed` |
| 4 | فحص طلب الموافقة | يظهر `JOIN_REQUEST_SENT` ويستمر الرابط التالي دون انتظار |
| 5 | فحص رابط منتهي | يظهر `FAILED` مع `LINK_INVALID_OR_EXPIRED` أو السبب الفعلي |
| 6 | فحص الفاصل | لا يبدأ الانضمام التالي قبل `min_delay_seconds` من آخر انضمام فعلي |
| 7 | فحص الثواني | تعرض اللوحة `join_started_at` و`join_completed_at` بدقة الثواني |
| 8 | إغلاق Dashboard | يستمر Worker في الخلفية |
| 9 | إعادة فتح Dashboard | يظهر نفس `cycle_id` والعداد الحالي، وليس صفرًا |
| 10 | Restart عند 17/30 | تستعاد الدورة من 17/30 أو من الحالة المسجلة، ولا تبدأ عشوائيًا |
| 11 | الوصول إلى 30/30 | تنتقل الدورة إلى `RESTING` ولا توجد عملية رقم 31 |
| 12 | موعد الدورة التالية | ينفذ `advance_link_import_cycle` تلقائيًا |
| 13 | حسابان متصلان | لكل حساب دورة وعداد وموعد مستقل |
| 14 | Job مكرر | لا ينفذ الرابط مرتين ولا يزيد العداد مرتين |
| 15 | Rate Limit | يتوقف الحساب للحماية وتنتقل العمليات للمراجعة |

---

## 14. ما يجب على Chat 4 مراجعته تحديدًا

يرجى من Chat 4 مراجعة الكود والملف على أنه تدقيق هندسي، وليس مجرد تدقيق في تنسيق الواجهة. يجب أن يراجع النقاط التالية:

1. هل يتم إنشاء دورة واحدة صحيحة لكل حساب داخل المهمة، وهل يمكن لطلبين متزامنين إنشاء دورتين للحساب نفسه بسبب غياب قفل أثناء `ensureAccountCycle`؟

2. هل `link_import_cycles` مرتبطة بالمهمة والحساب فقط، أم يجب أن تكون هناك طبقة دورة عالمية على مستوى الحساب تمنع تعارض مهمتين مستقلتين تستخدمان الحساب نفسه في الوقت نفسه؟

3. هل يتم احتساب كل نتيجة نهائية مرة واحدة فقط، بما فيها النجاح قبل انتهاء مراحل النشر والخروج، والاستعادة بعد Crash، والتقييد الذي ينقل عدة عمليات إلى `review`؟

4. هل `JOIN_REQUEST_SENT` ممثلة بطريقة متناسقة بين `operation.status` و`join_status` و`whatsapp_links.processing_status` وعدادات Dashboard؟

5. هل وظيفة `maybeCloseCycle` تمنع العملية رقم 31 في كل المسارات، بما فيها Recovery وRetry وManual Resume؟

6. هل Job الدورة المؤجلة يبقى محفوظًا إذا أعيد تشغيل السيرفر قبل موعده؟ وهل توجد معالجة تمنع إنشاء Wakeup Jobs مكررة؟

7. هل استعادة عملية `processing` بعد انتهاء Lease تمنع إعادة إرسال الرابط إذا كان `groupAcceptInvite` قد نجح قبل Crash؟

8. هل الاستعلامات تعمل على PostgreSQL قديم يحتوي على البيانات السابقة قبل إنشاء `link_import_cycles`، وهل Migration آمنة عند تكرار تشغيلها؟

9. هل استعلام `cycleStates` في Dashboard يعرض الدورة الصحيحة للمستخدم الصحيح، ولا يكشف دورة حساب لمستخدم آخر؟

10. هل تزامن Worker الافتراضي 3 مناسب، وهل قفل الحساب يغطي كل مسارات تنفيذ العملية وإعادة الجدولة؟

11. هل فشل الرابط لا يوقف الدورة، بينما تقييد الحساب يوقف الحساب بالطريقة المقصودة؟

12. هل الحساب الذي لديه أقل من 30 رابطًا يدخل `RESTING` ثم يُغلق بشكل صحيح عند عدم وجود روابط جديدة، أم ينشئ دورات فارغة بلا نهاية؟

13. هل Dashboard تفرق بوضوح بين `RESTING` و`STOPPED` و`SERVER_OFFLINE` و`ACCOUNT_DISCONNECTED`؟

14. هل توجد اختبارات تكامل حقيقية لمسار 30 نجاحًا، و30 فشلًا، وحالة مختلطة 18/7/5، وإغلاق Dashboard، وإعادة تشغيل السيرفر؟

15. هل الفاصل الزمني يقاس من وقت النتيجة الحالية حسب المطلوب، وليس فقط من وقت بدء العملية السابقة؟

---

## 15. Prompt جاهز لإرساله إلى Chat 4

```text
راجع مستودع x781780889-jpg/whatsapp-dashboard-new وخصوصًا آخر Commit:
01fdebe feat: add persistent per-account hourly join cycles

المطلوب مراجعة هندسية عميقة لقسم أتمتة الانضمام لروابط WhatsApp، وليس مراجعة شكلية للواجهة.

تحقق من الآتي:

1. صحة إنشاء دورة مستقلة لكل account.
2. الحد الأقصى 30 عملية لكل حساب خلال نافذة 60 دقيقة.
3. عدم تنفيذ الرابط رقم 31 داخل الدورة.
4. احتساب JOINED وJOIN_REQUEST_SENT وFAILED ضمن Processed مرة واحدة فقط.
5. استمرار الدورة عند الفشل وعدم إعادة الرابط بلا نهاية.
6. عدم انتظار موافقة مشرف المجموعة.
7. قياس الفاصل من النتيجة/الإكمال الفعلي للعملية الحالية.
8. احترام min_delay وmax_delay وتوزيع العمليات عشوائيًا داخل الساعة.
9. حفظ cycle_id وprocessed_count وremaining_count وcurrent_link وlast_result وnext_run_at.
10. RESTING ثم AUTO RESUME بعد انتهاء الساعة.
11. استمرار التنفيذ عند إغلاق Dashboard أو المتصفح.
12. استعادة الحالة بعد Restart أو Crash أو Deployment.
13. منع duplicate jobs والعمليات المكررة والتكرار بعد Recovery.
14. صحة القفل على مستوى الحساب عند وجود عدة Workers أو عدة مهام.
15. دقة حالة كل رابط في Dashboard وأسباب الفشل.
16. صحة Migration PostgreSQL وقابلية إعادة تشغيلها.
17. صحة صلاحيات استعلام cycleStates وعدم تسريب بيانات حسابات المستخدمين.
18. صحة تصنيف account_offline وinvalid_link وpending_approval وrate_limited وaccount_restricted وtimeout وalready_joined وJOINED_UNVERIFIED.
19. إضافة اختبارات تكامل حقيقية للحالات التالية:
   - 30 JOINED ثم RESTING.
   - 30 FAILED ثم RESTING.
   - 18 JOINED + 7 JOIN_REQUEST_SENT + 5 FAILED = 30.
   - إغلاق Dashboard ثم إعادة فتحه.
   - Restart عند 17/30 واستكمال الدورة.
   - Job مكرر لنفس الحساب والرابط.

أخرج النتيجة في أربعة أقسام:

A. ما هو صحيح ومكتمل.
B. المشاكل أو الثغرات التي وجدتها، مع درجة الخطورة.
C. الملفات والأسطر التي تحتاج تعديلًا.
D. Prompt تنفيذي دقيق يمكن إرساله إلى المطور لإصلاح كل نقص، مرتبة حسب الأولوية.

لا تكتفِ بقول إن الكود يبدو صحيحًا. اربط كل نتيجة بمسار التنفيذ أو استعلام قاعدة البيانات أو اختبار واضح.
```

---

## 16. نقاط مراجعة معروفة قبل اعتماد النظام نهائيًا

هناك نقاط يجب أن تُراجع في اختبار حي أو في تدقيق Chat 4 قبل اعتبار النظام نهائيًا بنسبة 100%.

أولًا، الكود الحالي يحفظ الدورات مرتبطة بـ`task_id` و`account_id`. هذا مناسب لتشغيل مهمة واحدة متصلة، لكنه يستحق مراجعة إضافية إذا كان المنتج يسمح بإنشاء مهمتين متزامنتين للحساب نفسه؛ حينها قد تكون هناك حاجة إلى قفل دورة عالمي على مستوى الحساب والمستخدم قبل إنشاء مهمة جديدة.

ثانيًا، اختبارات Jest تثبت سياسة الدورة وتصنيف النتائج وسلامة الوحدات، لكنها لا تنفذ PostgreSQL وBullMQ وWhatsApp حقيقيين في مسار ساعة كامل. لذلك تبقى اختبارات Restart وClose Dashboard و30/30 اختبارات قبول حي يجب إجراؤها بعد النشر.

ثالثًا، حماية الحساب عند Rate Limit أو Forbidden مقصودة، لكنها تحتاج إلى التأكد من أن كل العمليات التي نُقلت إلى `review` لا تترك عداد دورة عالقًا في الإنتاج. يجب فحص `cycle_counted_at` و`processed_count` بعد تجربة تقييد مصطنعة في بيئة اختبار.

رابعًا، نجاح `groupAcceptInvite` مع `JOINED_UNVERIFIED` يعالج الفشل الزائف السابق، لكنه لا يستطيع إجبار WhatsApp على إظهار بيانات العضوية فورًا. يجب الحفاظ على الفرق بين النجاح التشغيلي والقراءة التفصيلية للبيانات.

---

## 17. الملفات والـCommits

### الملفات المعدلة في آخر تطوير للدورات

| الملف | التعديل |
|---|---|
| `backend/src/api/services/LinkImportService.js` | محرك الدورات، التخصيص، العدادات، الاستراحة، الاستئناف، Recovery، ومنع العملية رقم 31 |
| `backend/src/api/services/JoinCyclePolicy.js` | سياسة الحد والتصنيف والحساب القابل للاختبار |
| `backend/src/api/services/JoinScheduler.js` | توزيع التأخير العشوائي داخل الوقت المتبقي |
| `backend/src/database/LinkImportMigrations.js` | جدول الدورات والحقول والفهارس وMigration القديمة |
| `backend/src/lib/QueueManager.js` | Job إيقاظ الدورة وتزامن Worker المستقل لكل حساب |
| `backend/index.js` | تسجيل Handler للدورة التالية |
| `backend/src/api/controllers/TelegramController.js` | إرجاع حالات الدورات وإعدادات 30/60 |
| `frontend/src/views/WhatsAppJoinAutomationView.tsx` | بطاقات الحسابات، العدادات، RESTING، العملية التالية، ورسائل الأحداث |
| `backend/src/api/services/JoinCyclePolicy.test.js` | اختبارات سياسة الدورة |
| `backend/src/api/services/LinkImportService.test.js` | اختبار توزيع الفاصل داخل نافذة الدورة |

### التسلسل المختصر للتعديلات السابقة

| Commit | المعنى |
|---|---|
| `8180505` | اعتبار قبول دعوة WhatsApp نجاحًا مكتملًا عند تأخر بيانات العضوية |
| `f92dc15` | فرض الفاصل الأدنى على مستوى الحساب وإظهار الثواني |
| `d0e46c3` | قياس الفاصل من آخر إكمال فعلي للانضمام |
| `94bf2b7` | توثيق المشاكل والإصلاحات السابقة |
| `01fdebe` | إضافة الدورات الدائمة لكل حساب والاستئناف والعدادات والواجهة |

---

## 18. الخلاصة النهائية

المشكلة لم تكن في زر الانضمام وحده. كانت في التوافق بين نتيجة WhatsApp وبين فحص العضوية، وبين العملية وقاعدة البيانات، وبين Outbox وQueue وWorker، وبين وقت إنشاء المهمة ووقت الانضمام الحقيقي، وبين عداد النجاح وعدد المحاولات الفعلية.

تمت معالجة المسار بحيث يصبح قبول الدعوة من WhatsApp نجاحًا تشغيليًا، ويُحتفظ بتأخر التحقق كحالة `JOINED_UNVERIFIED` بدل فشل زائف. كما أصبحت طلبات الموافقة تُسجل كـ`JOIN_REQUEST_SENT` وتُحتسب ضمن الدورة دون انتظار. وأصبح الفشل النهائي يُسجل مع سببه ولا يوقف الدورة، بينما يتوقف الحساب فقط عند إشارات الحماية أو التقييد الخطيرة.

أضيف نظام دورة دائم لكل حساب بحد 30 رابطًا خلال 60 دقيقة، مع توزيع زمني عشوائي بعد كل نتيجة، وعدادات منفصلة للنجاح والطلبات والفشل، وحالة `RESTING`، وموعد `next_cycle_at`، وJob للاستئناف التلقائي. وتم ربط ذلك بقاعدة بيانات وWorker مستقلين عن Dashboard، مع Recovery وLocks وIdempotency لمنع فقدان أو تكرار العمليات.

آخر نسخة مرفوعة هي `01fdebe`. قبل الاعتماد النهائي، يجب نشرها على Railway وإجراء اختبار حي محدود، ثم إرسال هذا الملف والـPrompt المرفق إلى Chat 4 ليقدم مراجعة ثغرات ونواقص قابلة للتنفيذ، وبعدها يمكن تطبيق أي Prompt تصحيحي ينتج عن المراجعة.

## المراجع داخل المستودع

[1]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/api/services/GroupJoinerService.js "تنفيذ الانضمام والتحقق من العضوية"

[2]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/api/services/LinkImportService.js "محرك العمليات والدورات والـRecovery"

[3]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/api/services/JoinScheduler.js "محرك التأخير والفاصل الزمني"

[4]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/api/services/JoinCyclePolicy.js "سياسة الدورة وتصنيف النتائج"

[5]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/database/LinkImportMigrations.js "مخطط الدورات والعمليات"

[6]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/backend/src/lib/QueueManager.js "Queue وWorker عمليات الروابط"

[7]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/blob/main/frontend/src/views/WhatsAppJoinAutomationView.tsx "واجهة Dashboard وبطاقات الحسابات"

[8]: https://github.com/x781780889-jpg/whatsapp-dashboard-new/commit/01fdebe "آخر تطوير للدورات الدائمة لكل حساب"
